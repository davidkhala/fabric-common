matrix:
  include:
    - language: node_js
      node_js:
        - "8"
      dist: bionic
      install:
        - ./install.sh travis
        - cd nodejs; npm install; cd -;
      script:
        - node nodejs/test



